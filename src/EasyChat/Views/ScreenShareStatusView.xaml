<Window x:Class="EasyChat.Views.ScreenShareStatusView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="420"
        Height="72"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        Loaded="Window_Loaded"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border CornerRadius="8"
            Background="#EE1F2937"
            BorderBrush="#55FFFFFF"
            BorderThickness="1"
            Padding="12,10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <Ellipse x:Name="AccentDot"
                             Width="8"
                             Height="8"
                             Margin="0,0,8,0"
                             VerticalAlignment="Center" />
                    <TextBlock Text="正在共享屏幕"
                               Foreground="White"
                               FontWeight="SemiBold"
                               FontSize="13" />
                    <TextBlock x:Name="ResolutionText"
                               Foreground="#D1D5DB"
                               FontSize="12"
                               Margin="8,1,0,0" />
                </StackPanel>
                <TextBlock x:Name="ControllerText"
                           Text="无人控制"
                           Foreground="#CBD5E1"
                           FontSize="12"
                           Margin="16,4,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">
                <Button x:Name="DisconnectControlButton"
                        Content="断开控制"
                        Width="88"
                        Height="30"
                        Margin="0,0,8,0"
                        ToolTip="断开当前远程控制"
                        Click="DisconnectControlButton_Click"
                        IsEnabled="False" />
                <Button Content="停止共享"
                        Width="88"
                        Height="30"
                        Background="#EF4444"
                        Foreground="White"
                        ToolTip="停止共享屏幕"
                        Click="StopShareButton_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
